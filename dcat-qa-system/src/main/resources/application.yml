

es:
  host: localhost
  port: 3030
  property_index: dcatprops
  launuts_index: launuts
  language_index: lang
  theme_index: themes
  license_index: licenses
  frequency_index: frequencies
  filetype_index: filetypes


data-path: src/data

ontology: ${data-path}/dcat+labels.json

launuts: ${data-path}/launuts.ttl

templates: ${data-path}/templates.txt

languageEntities: ${data-path}/languages-skos-ap-act.rdf

themeEntities: ${data-path}/data-theme-skos-ap-act.rdf

licenseEntities: ${data-path}/licences.json

frequencyEntities: ${data-path}/frequencies-skos.rdf

filetypeEntities: ${data-path}/filetypes.json

sparql:
  endpoint: https://openbot.cs.upb.de/fuseki/opal2020-07/query

properties:
  languages: "en, de"

logging:
  level:
    com:
      martenls:
        DEBUG